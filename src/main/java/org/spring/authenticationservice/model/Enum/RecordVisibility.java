package org.spring.authenticationservice.model.Enum;

public enum RecordVisibility {

    PUBLIC, // Visible to all users
    PRIVATE, // Visible only to the patient
    ADMIN // Visible only to the admin
}
